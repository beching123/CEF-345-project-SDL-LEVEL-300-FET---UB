# ========================================
# DOCKER DEPLOYMENT ENVIRONMENT VARIABLES
# ========================================
# Copy to .env when deploying with Docker

NODE_ENV=production
FRONTEND_URL=http://localhost
REACT_APP_API_URL=
REACT_APP_ENVIRONMENT=docker
